sklearn library is reuired to run the program

plots and confusion matrix are saved to plots

First the random forest is build and results are displayed in the terminal window followed by gradien boosting.

the validation curve code is commented out because it takes over 3 hrs to run.